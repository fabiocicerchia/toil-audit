name: security

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 6 * * 1" # weekly, catches newly disclosed CVEs

permissions:
  contents: read
  security-events: write # upload SARIF to the Security tab

jobs:
  secrets:
    name: secret scan (gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  vulns:
    name: filesystem & dependency scan (trivy)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Trivy scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scanners: vuln,misconfig,secret
          format: sarif
          output: trivy.sarif
          severity: CRITICAL,HIGH
      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v4.37.4
        with:
          sarif_file: trivy.sarif

  # CodeQL is language-specific. Uncomment and set languages once the repo
  # has code. Supported: c-cpp, csharp, go, java-kotlin, javascript-typescript,
  # python, ruby, swift.
  #
  # codeql:
  #   name: codeql
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       language: [javascript-typescript]
  #   steps:
  #     - uses: actions/checkout@v7
  #     - uses: github/codeql-action/init@v3
  #       with:
  #         languages: ${{ matrix.language }}
  #     - uses: github/codeql-action/analyze@v3
