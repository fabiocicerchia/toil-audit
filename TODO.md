# TODO

Open items only. Completed work is dropped from here — the CHANGELOG
is the record of what shipped.

- [ ] GitLab CI and Jenkins ingestion.
- [ ] Pull logs directly via the API instead of a JSON export.
- [ ] Trend mode: monthly toil delta after fixes land, to show the ROI.
- [ ] Flaky-test attribution: which test file causes the red→green loops.
- [ ] Scheduled runs: weekly toil report per repo, Slack delivery.
