# Read the Docs builds the site itself (.readthedocs.yaml -> this file). It does
# not run the ghcr.io/fabiocicerchia/mkdocs-material-pinned image, so the pins
# have to exist here too — that duplication is RTD's, not ours to remove.
#
# Keep this to what mkdocs.yml actually loads. minify, redirects, awesome-pages,
# git-revision-date and mike are in the image for the repos that opt into them;
# installing them here bought nothing but build minutes.
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
pymdown-extensions==11.0.1
