# The pinned docs toolchain, installed by .github/workflows/docs.yml.
#
# Keep this to what mkdocs.yml actually loads. git-revision-date-localized
# is not optional: mkdocs.yml enables it, and `mkdocs build --strict`
# aborts with a configuration error when it is missing.
mkdocs==1.6.1
mkdocs-git-revision-date-localized-plugin==1.5.3
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
pymdown-extensions==11.0.1
